use raylib::RaylibHandle;

use crate::memory::Memory;

const DISPLAY_WIDTH: usize = 480;
const DISPLAY_HEIGHT: usize = DISPLAY_WIDTH / 4 * 3;

pub fn render(raylib_handle: &mut RaylibHandle, memory: &Memory) {}
